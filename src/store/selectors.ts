// import { State } from '../types/state';
// import { OfferType } from '../types';

// const getCity = (state: State): string => state.city;

// const getOffers = (state: State): OfferType[] => state.offers;

// const getOffersByCity = (state: State): OfferType[] => {
//   const city = getCity(state);
//   const offers = getOffers(state);
//   return offers.filter((offer) => offer.city.name === city);
// };

// export {getCity, getOffers, getOffersByCity};
//TODO файл на удаление весь
